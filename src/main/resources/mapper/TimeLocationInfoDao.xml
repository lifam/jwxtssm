<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jwxtssm.dao.TimeLocationInfoDao">
    <insert id="addTimeLocationInfo" parameterType="TimeLocationInfo">
        insert into location_info values (#{locationId}, #{courseId}, #{time});
    </insert>
</mapper>