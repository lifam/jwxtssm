<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jwxtssm.dao.IBasicInfo">
    <insert id="addBasicInfo" parameterType="BasicInfo" useGeneratedKeys="true" keyProperty="basic_id">
        insert into basic_info(head_img, vice_id, name, password, sex, height, weight, birth_info, home_address, formal_id, reward_info, punishment_info, info_transparency)
    </insert>
</mapper>